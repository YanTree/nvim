{
    "packages": {
        "prefix": "pack",
        "body": [
            "-- ${1:sort}/${2:name}/packages.lua\n",
            "cat.packages[\"${3:repository}\"] = {",
            "\tpin = true,",
            "\tcommit = \"${4:sha-hvalue}\",",
            "}\n"
        ],
        "description": "Template of create config file"
    },
    "config": {
        "prefix": "conf",
        "body": [
            "-- ${1:sort}/${2:name}/config.lua\n",
            "local config = {",
            "\tlazy = true,",
            "\tcmd = { \"\" },",
            "\tevent = { \"BufReadPre\" },",
            "\topts = {",
            "\t},",
            "\tconfig = function(_, opts)",
            "\t\trequire(\"\").setup(opts)",
            "\tend,",
            "}\n",
            "cat.config[\"\"] = config",
            "cat.set_options(\"\")\n"
        ],
        "description": "Template of create config file"
    }
}
